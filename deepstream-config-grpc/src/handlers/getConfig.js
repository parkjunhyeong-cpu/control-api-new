import { getConfig } from "../constants/configStore.js";

export function getConfigHandler(call, callback) {
  // TODO: 현재 config 를 { input: getConfig() } 형태로 callback(null, response) 호출
  callback(new Error("getConfigHandler() not implemented"));
}
